🔧 УСТАНОВКА MYSQL LOCАЛЬНО НА WINDOWS
==========================================

## ШАГИ УСТАНОВКИ:

### 1. Скачайте MySQL Server 8.0
- Откройте: https://dev.mysql.com/downloads/mysql/
- Нажмите "Download" на MySQL Server 8.0.x  
- Выберите "Windows (x86, 64-bit), MSI Installer"
- Нажмите "Download" (файл ~330 МБ)

### 2. Установите MySQL
- Откройте скачанный файл `.msi`
- Нажмите "Next" несколько раз
- ROOT PASSWORD: установите пароль (например: `mysql123`)
- Запомните пароль!
- Дождитесь завершения установки

### 3. Проверка установки
```powershell
mysql --version
```

### 4. Подключение к MySQL
```powershell
mysql -u root -p
```
Введите пароль.

### 5. Создание БД
```sql
CREATE DATABASE evrocontayner;
EXIT;
```

---

## ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

- Добавьте путь в PATH (если mysql не найдена):
```powershell
$env:Path += ";C:\Program Files\MySQL\MySQL Server 8.0\bin"
```

Дальше обратитесь к главному README.md
